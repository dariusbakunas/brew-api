enum RecipeType {
  ALL_GRAIN
  EXTRACT
  PARTIAL_MASH
  CIDER
  MEAD
  WINE
}

type Recipe {
  id: ID!
  name: String!
  description: String
  type: RecipeType!
  batchSize: Float!
  boilTime: Float!
  source: String
  createdBy: LimitedUser!
}

extend type Query {
  recipes: [Recipe!]!
}
