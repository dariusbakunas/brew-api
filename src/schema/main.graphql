directive @hasScope(scope: [String]) on FIELD_DEFINITION

enum SortDirection {
  ASCENDING
  DESCENDING
}

type PageInfo {
  currentCursor: String
  nextCursor: String
}

type Query {
  _empty: String,
}

type Mutation {
  _empty: String,
}
