type Query {
  countries: [Country!]!
  hops: [Hop!]!
  users: [User!]!
  userByEmail(email: String!): User
}

type Mutation {
  createHop(input: HopInput!) : Hop!
  createUser(input: UserInput!) : User!
}
