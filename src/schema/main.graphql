# import Country from 'src/schema/country.graphql'
# import Hop from 'src/schema/hop.graphql'
# import FlavorProfile from 'src/schema/flavorProfile.graphql'

type Query {
  countries: [Country!]!
  hops: [Hop!]!
  flavorProfiles: [FlavorProfile!]!
}
