apiVersion: v1
kind: Service
metadata:
  name: {{ include "brew-api.fullname" . }}
  labels:
{{ include "brew-api.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "brew-api.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
