apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brew-api.fullname" . }}-test-connection"
  labels:
{{ include "brew-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "brew-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
